apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Cove application services. Each service lives in its own subdirectory;
# overlays set environment-specific values (image tag, replica count, etc.).
resources:
    - cove-api/
